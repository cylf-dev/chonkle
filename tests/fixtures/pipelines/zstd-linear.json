{
  "codec_id": "zstd-linear",
  "direction": "encode",
  "inputs": {"bytes": {"type": "bytes"}},
  "constants": {
    "level": {"type": "int", "value": 3}
  },
  "outputs": {
    "bytes": "zstd.bytes"
  },
  "steps": [
    {
      "name": "zstd",
      "codec_id": "zstd",
      "src": "file:///path/to/codecs/zstd-rs/target/wasm32-wasip2/release/zstd_rs.wasm",
      "inputs": {
        "bytes": "input.bytes",
        "level": "constant.level"
      },
      "outputs": ["bytes"],
      "encode_only_inputs": ["level"]
    }
  ]
}
